server:
  port: 5000

web:
  resources:
    add-default-mappings: false

infra:
  profiles:
    active: dev
  cloud:
    discovery:
      enabled: false


config:
  server:
    etcd:
      endpoints: "http://localhost:2379"
      username: "root"
      password: "88888888"
      namespace: ${app.name}/

app:
  name: demo
