service:
  provider:
    uri: '/provider'

registry:
  http-url: 'http://localhost:5000/services'

  services:
    uri: '/services'

infra:
  profiles:
    active: dev

logging:
  level:
    cn.taketoday.cloud.protocol: debug
