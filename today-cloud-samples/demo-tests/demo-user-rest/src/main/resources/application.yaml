
infra:
  profiles:
    active: dev

logging:
  level:
    root: info
    web: trace
    cn.taketoday.demo: debug
    infra.cloud.protocol: debug


server:
  netty:
    shutdown:
      quiet-period: 0
      unit: seconds
      timeout: 2

today:
  service-registry:
    simple:
      uri: http://localhost:9000
