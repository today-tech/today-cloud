
infra:
  profiles:
    active: dev
  cloud:
    service-registry:
      auto-registration:
        fail-fast: true

      simple:
        instance-port: 8080
      uri: http://localhost:9000

service:
  server:
    port: 8080

logging:
  level:
    root: info
    web: trace
    cn.taketoday.demo: debug
    infra.cloud.provider: debug